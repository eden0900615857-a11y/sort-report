# 排序報告

學號：11320335

姓名：陳竣翊

模擬頁面：http://127.0.0.1:5500/page.html


## 氣泡排序法

氣泡排序（Bubble Sort）是一種簡單的交換排序。透過反覆比較相鄰元素，若順序錯誤則交換，讓比較大的元素像氣泡一樣慢慢浮到序列末端。

- 原理：比較相鄰元素，若前者大於後者則交換；每一輪把最大值放到最後。
- 最好情況：O(n)（已排序時，只需一次比較）
- 平均情況：O(n²)
- 最壞情況：O(n²)

## 選擇排序法

選擇排序（Selection Sort）是一種簡單的排序方法，每次從未排序區間選出最小（或最大）元素，並將其放到已排序區間的末端。

- 原理：將整個陣列分成已排序和未排序兩部分，每次在未排序部分選出最小元素交換到前面。
- 最好情況：O(n²)
- 平均情況：O(n²)
- 最壞情況：O(n²)

## 插入排序法

插入排序（Insertion Sort）將資料分成已排序和未排序兩部分，逐一將未排序元素插入到已排序部分的正確位置。

- 原理：從第二個元素開始，將當前元素與前面的已排序區間比較，插入適當位置。
- 最好情況：O(n)（已排序時）
- 平均情況：O(n²)
- 最壞情況：O(n²)

## 合併排序法

合併排序（Merge Sort）是一種分治演算法，先遞迴地將陣列分成兩半，排序後再合併成單一已排序序列。

- 原理：分而治之，先將陣列分成小片段，再逐步合併成已排序陣列。
- 最好情況：O(n log n)
- 平均情況：O(n log n)
- 最壞情況：O(n log n)

## 快速排序法

快速排序（Quick Sort）是一種分治演算法，選定一個 pivot 進行分區，將小於 pivot 的元素移到左側，大於 pivot 的元素移到右側，然後對左右子區間遞迴排序。

- 原理：選擇一個 pivot，將陣列分成左右兩個區間，遞迴排序後組合成已排序陣列。
- 最好情況：O(n log n)
- 平均情況：O(n log n)
- 最壞情況：O(n²) - 當 pivot 選得極度不平衡時
- 優點：平均效率高，實際表現通常優於 O(n²) 排序；原地排序可節省空間。
- 缺點：最壞情況時間複雜度較差，且遞迴深度可能造成額外開銷。

## 綜合比較

比較各排序演算法的特性與適用場合：

- 氣泡排序：實作簡單，適合教學與小型資料，不適合大量資料。
- 選擇排序：交換次數較少，但比較次數仍為 O(n²)，適合理解選擇過程。
- 插入排序：對近乎已排序資料表現較佳，適合小型陣列與線上資料插入場景。
- 合併排序：穩定且時間複雜度穩定為 O(n log n)，適合大型資料排序，但需額外空間。
- 快速排序：平均表現優異，適合大量資料排序；最壞情況為 O(n²)，但可透過隨機化 pivot 或三數取中法改善。

## 學習心得

透過這個專案，我了解了多種排序演算法的實作差異與運作流程。互動式視覺化幫助我更直觀地理解比較、交換與合併的過程，同時也學會了如何比較演算法時間複雜度與空間複雜度。
